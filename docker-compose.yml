version: "3.9"
services:
  web:
    build: .
    ports:
      - "8090:8090"
    env_file:
      - .env
    environment:
      - NXTGEN_DB_PATH=/app/data/nxtgen.db
    volumes:
      - nxtgen_data:/app/data
    restart: unless-stopped

volumes:
  nxtgen_data:
